# way too minimal code (kept intentionally), variable rect is not even defined. File only for tasks breaking down purposes
# I include this in commit to keep track of the progress

import cv2
import numpy as np
from pywinauto import Application
import pyautogui
# screenshot = pyautogui.screenshot(region=(rect.left, rect.top, rect.width(), rect.height()))